The raw data is on HeiBox, protected by password (the same password as we used before), as it 
contains internal lab and order IDs, which (even though they are not linked to patient IDs) should
still be remvoed before making them publicly available.
